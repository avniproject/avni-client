import StubbedBaseService from "./StubbedBaseService";

class StubbedGroupSubjectService extends StubbedBaseService {
    constructor(serviceData) {
        super(serviceData);
    }

    populateGroups() {}
}

export default StubbedGroupSubjectService;
