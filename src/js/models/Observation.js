class Observation {
    static schema = {
        name: 'Observation',
        properties: {
            conceptUUID: 'string',
            valueJSON: 'string'
        }
    };
}

export default Observation;