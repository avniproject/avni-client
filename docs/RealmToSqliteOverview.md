# Realm → SQLite Migration — Technical Team Overview

## Companion Docs

| Doc | Scope |
|---|---|
| [`repository-migration-guide.md`](./repository-migration-guide.md) | The repository abstraction layer API and how services migrate onto it. |
| [`RuleEngineCompatibility.md`](./RuleEngineCompatibility.md) | Which Realm query patterns work on SQLite; SQL translation vs JS fallback. |
| [`RuleSqlMigrationGuide.md`](./RuleSqlMigrationGuide.md) | The `execReport` / `exec*` SQL fast path for report-card rules. |
| [`RealmToSqliteMigrationChecklist.md`](./RealmToSqliteMigrationChecklist.md) | Per-org migration testing checklist (make targets + comparison scripts). |

## TL;DR

We replaced the Realm database backend with SQLite (via [op-sqlite](https://github.com/OP-Engineering/op-sqlite), with SQLCipher for encryption) **without changing any model or service code**. Services now go through a repository abstraction; the repository is backed by a proxy (`SqliteProxy`) that implements Realm's object/results/query contract on top of SQLite. Per-user activation is driven by a server-side **"SQLite Migration"** user group; the backend switch happens mid-sync without double-syncing.

Shape of the change:

```
Service
   ↓ (unchanged)
BaseRepository — RealmRepository
   ↓ (unchanged)
db  ← Realm  OR  SqliteProxy ─┬─► op-sqlite (SQLCipher) ─► avni_sqlite.db
                              ├─► EntityHydrator (row → entity)
                              ├─► RealmQueryParser (query string → SQL)
                              └─► JsFallbackFilterEvaluator (un-translatable ops)
```

## The `framework/db/` Module

Everything SQLite-specific lives in `packages/openchs-android/src/framework/db/`. One-line responsibilities:

| File | Responsibility |
|---|---|
| `RealmFactory.js` | Opens Realm with key from `EncryptionService`. Unchanged path. |
| `SqliteFactory.js` | Opens op-sqlite with SQLCipher key, runs Drizzle migrations, seeds `Settings` / `UserInfo`, returns `SqliteProxy`. |
| `SqliteProxy.js` | Drop-in replacement for `RealmProxy`. Implements `objects/create/write/delete/objectForPrimaryKey`, plus `bulkCreate` (batched INSERTs) and `exec*` SQL-native methods. |
| `SqliteResultsProxy.js` | Drop-in for `RealmResults`. Lazy query builder — chained `.filtered()` / `.sorted()` don't execute until you read a row. |
| `EntityHydrator.js` | Flat SQL rows → nested objects. Resolves FK columns, parses embedded JSON, attaches child lists. Has a session-scoped cache and a batch preload path. |
| `RealmQueryParser.js` | Translates Realm query strings to SQL WHERE. Routes patterns that can't be translated (SUBQUERY, `@count`, `DISTINCT`, `ANY`, `@links`) to the JS fallback. |
| `JsFallbackFilterEvaluator.js` | Post-filter over hydrated entities for the un-translatable patterns above. |
| `SchemaGenerator.js` | Realm schema definitions → SQL `CREATE TABLE` metadata (column types, FKs, embedded-property tracking). |
| `DrizzleSchemaExport.js` | Realm schemas → Drizzle `sqliteTable()` definitions. Consumed only by `drizzle-kit` at generation time — not shipped to device. |
| `MigrationRunner.js` | Runs bundled Drizzle migrations on device startup. Tracks version in `schema_version`. |
| `SqliteUtils.js` | Conversion helpers: `camelToSnake`, `schemaNameToTableName`, `normalizeRealmType`. |
| `migrations/drizzleMigrations.js` | Auto-generated by `scripts/bundle-migrations.js`. Exports journal + SQL files map. |

## Backend Selection & Activation

- **Both** backends are initialised at startup in `GlobalContext.initialiseGlobalContext()` (`src/GlobalContext.js`). Neither is gated behind a static `USE_SQLITE` flag any more.
- The **active backend** is read from AsyncStorage (`avni.sqliteMigration.<username>`), defaults to Realm.
- Activation is driven by membership in the server-side **"SQLite Migration"** user group (UUID `e6e5e4e3-e2e1-4f00-8000-d0d1d2d3d4d5`). Delivered via the existing `MyGroups` sync entity — **no new server-side contracts**.
- `SqliteMigrationService` owns the state machine:
  ```
  idle → pending_upload → pending_target_sync → completing → idle
  ```
  Each step is persisted per-username so a crash mid-migration resumes cleanly. `resumeIfPending()` runs on startup.
- The **mid-sync backend switch** (`SyncService` + `SqliteMigrationService._runSync`) flips the active backend between `pending_upload` and `pending_target_sync` without a full sync reboot — so the user sees one continuous sync, not two.

**If you're touching sync or login flows:** check `SqliteMigrationService.checkAndMaybeMigrate()` to understand where the hook fires.

## Sync Flow Differences on SQLite

- **Bulk persist.** `SyncService.persistAll()` checks `this.db.isSqlite` and routes to `SqliteProxy.bulkCreate`, which issues a single `executeBatch` for the whole batch instead of one statement per entity.
- **Shallow hydration during sync.** `EntityHydrator.setShallowMode(true)` is toggled for the duration of sync so FK resolution doesn't hydrate child lists — sync only needs scalars + direct FK ids. Flipped back off when sync completes. **Carve-out:** `SubjectMigrationService.migrateSubjects` walks `subject.enrolments → enrolment.encounters → observation` arrays to delete entire subtrees, so shallow mode is paused around that step (`SyncService.js:285-295`) and re-enabled after. As a result, the `"SQLite shallow hydration disabled" / "enabled"` debug logs fire **twice** in a normal sync (once around subject migration mid-sync, once at the end) — that's expected, not a bug.
- **Reference-data cache built before post-sync reset.** `SyncService._buildReferenceCacheIfSqlite()` (called before `resetServicesAfterFullSyncCompletion`) pre-populates `EntityHydrator.referenceDataCache` with AddressLevel / Concept / SubjectType / etc. Without this, form hydration after sync pulled depth-0 stubs and rendered stale forms.
- **FK enforcement during sync.** `SyncService._disableForeignKeysIfSqlite` toggles `PRAGMA foreign_keys = OFF` for the duration of every sync run, and `_enableForeignKeysIfSqlite` flips it back `ON` after. Both go through `RepositoryFactory.setForeignKeysEnabled(...)` so the no-op-on-Realm and PRAGMA-on-SQLite branching lives at the repository layer. Sync **is** ordered parent-first by design — the toggle is purely a safety net so an unexpected ordering bug surfaces as a logged violation rather than aborting the user's sync mid-flight.
- **Post-sync FK integrity check.** `SyncService._checkForeignKeyIntegrityIfSqlite` (called once after sync completes) runs `PRAGMA foreign_key_check` via `RepositoryFactory.runForeignKeyCheck()` and reports any orphan rows. Violations log to `General.logError` *and* fire `ErrorUtil.notifyBugsnag(..., "SyncService::FKIntegrityCheck")` — that's where to look first when investigating, not in sync telemetry.
- **Count-only dashboard cards.** `MyDashboard` initial card counts use `SELECT COUNT(*)` via `SqliteResultsProxy.count()` rather than hydrating the full results set. Lists are lazy-loaded on tap.

## Hydration Modes & Depths

Hydration is the row → object step `EntityHydrator` performs when a query result is read. Two flags shape how much of the entity graph it materialises:

- **`depth: N`** — number of FK hops to traverse. At depth 0, FK columns resolve to a cached stub (scalars only, no further FK objects) or `null` if not cached; list children are left empty. At depth N, the hydrator recursively hydrates each FK target and list element with `depth: N-1`. Default is 3.
- **`skipLists: true | false`** — when true, list-typed properties (e.g. `Individual.encounters`, `FormElementGroup.formElements`) are left as `[]` instead of fetched. Independent of `depth`: you can ask for deep FK traversal but no list children, or for shallow FKs with full lists.

Two ways to opt into a non-default hydration:

- **Per query.** `results.withHydration({skipLists: true, depth: 1})` on a `SqliteResultsProxy`. The chained results proxy is still lazy — hydration options apply when rows are read. Used by search and any read path that needs the row but not its subtree.
- **Process-wide.** `db.setShallowMode(true)` flips the hydrator into a shallow default (`{skipLists: true, depth: 1}`) for *every* hydrate call until turned off. Used during sync because openchs-models' `fromResource` calls `findByKey("uuid", ...)` for each child; without shallow mode every call would batch-preload the parent's full subtree.

### Where each depth is used

| Depth + flag | Used by | Rationale |
|---|---|---|
| `depth: 0, skipLists: true` | `EntityHydrator._batchPreloadFkReferences` — the FK-preload step inside batch hydration | Cheap stub for join targets, promoted to richer hydration when the batch loader replaces it. **Footgun:** the depth-0 stub used to win the session-cache lookup against deeper later loads — see Footgun #2 below. |
| `depth: 1, skipLists: true` | `setShallowMode` default during sync (`EntityHydrator.js:112`); `SqliteProxy.DEFAULT_REFERENCE_CACHE_CONFIGS` (Gender, SubjectType, Program, EncounterType, AddressLevel — `SqliteProxy.js:106-110`); search results (`IndividualService.js:278`); internal lookup paths in `SqliteProxy.js:291,300,449` | Lookup-only — scalars + direct FK ids resolved as stubs, no child lists. The most common shallow shape. |
| `depth: 1, skipLists: false` | Post-sync ref cache for `ChecklistItemDetail` (`SyncService.js:786`) | Need child list but no further FK traversal. |
| `depth: 2, skipLists: false` | Post-sync ref cache for `Concept` (`SyncService.js:785`) | Concept → ConceptAnswer → Concept needs two hops to resolve answer concepts. |
| `depth: 3, skipLists: false` | Default for `SqliteResultsProxy` queries when no `withHydration()` call (`SqliteResultsProxy.js:91`); post-sync ref cache for `Form` (`SyncService.js:787`) | Form → FormElementGroup → FormElement → Concept; full hydration so views render the form complete. |

### Two reference caches

`buildReferenceCache(...)` is invoked twice with different configs:

- **Boot-time:** `SqliteProxy.DEFAULT_REFERENCE_CACHE_CONFIGS` (5 entries, all `depth: 1, skipLists: true`) is applied when the proxy is constructed if the relevant tables are non-empty (`SqliteProxy.js:121`). Covers the small reference entities every code path looks up by uuid (Gender, SubjectType, Program, EncounterType, AddressLevel).
- **Post-sync:** `SyncService._buildReferenceCacheIfSqlite()` (`SyncService.js:771-790`) extends the cache with a larger, mixed-depth set — adds OrganisationConfig / IndividualRelation* / GroupRole at depth 1, plus the deeper entries (Concept at depth 2, ChecklistItemDetail at depth 1 with lists, Form at depth 3 with lists). Runs *before* `resetServicesAfterFullSyncCompletion` so views that re-read forms after sync see fully populated subtrees instead of depth-0 stubs.

If you add a new schema that's frequently looked up by uuid, decide which list (if either) it joins, and pick the smallest depth that keeps the call sites happy — every hop is an extra round of FK resolution and (when `skipLists: false`) child-list queries.

## Encryption

`SqliteFactory` asks `EncryptionService.getEncryptionKey()` for the same key that has always backed Realm. Keychain-stored via `react-native-keychain`, converted to a hex string and passed to op-sqlite's SQLCipher config. Key rotation / anonymised-DB flows reuse the existing `EncryptionService.encryptRealm / decryptRealm` code paths.

## Schema & Migrations

Schema is hand-maintained upstream (in `openchs-models` via `EntityMappingConfig`) and translated into two SQLite shapes at build time:

- `SchemaGenerator.js` produces runtime `TableMeta` used by `SqliteProxy` / `EntityHydrator`.
- `DrizzleSchemaExport.js` produces `sqliteTable()` definitions for `drizzle-kit` to diff against.

Both read directly from `EntityMappingConfig`, so **the SQLite schema automatically tracks whatever the Realm schemas declare** — the dev never hand-writes a SQLite table definition. A `SchemaValidationTest` fails CI if the two generators drift.

### Turning a Realm schema change into a SQLite migration

When `openchs-models` ships a Realm schema change (new field, new schema, renamed property, FK change), you need one extra step in this repo to emit the matching SQLite migration:

1. Bump the `openchs-models` dependency in `packages/openchs-android/package.json` (and run `make deps`).
2. Generate + bundle the migration:
   ```
   make migration_generate name=<descriptive-name>
   ```
   This shells out to `packages/openchs-android/scripts/generate-migration-diff.sh`, which runs `npx drizzle-kit generate` (diffs the current `DrizzleSchemaExport` output against the previous snapshot in `drizzle-migrations/meta/`) and then runs `scripts/bundle-migrations.js` to fold the generated SQL into `src/framework/db/migrations/drizzleMigrations.js`.
3. Review both outputs:
   - `drizzle-migrations/<n>_<name>.sql` — the SQL that will run on device.
   - `src/framework/db/migrations/drizzleMigrations.js` — the bundled JS module (React Native can't read `.sql` files at runtime, so everything is embedded).
4. Commit both. `MigrationRunner.js` applies unseen migrations on next app start, tracking the applied version in the `schema_version` table.

Supporting targets:

```
make migration_bundle            # re-bundle without generating (rare — only after hand-editing SQL)
make migration_export_schema     # sanity check: prints the count of tables exported
```

### Footguns around migrations

- **Destructive auto-generation.** `drizzle-kit generate` does not ask before producing a `DROP COLUMN` or a column-rename-as-drop+add. Always read the generated SQL. If a rename is intended, hand-edit the migration or split the change across two migrations (add new, backfill, drop old).
- **Type-conversion drift.** `DrizzleSchemaExport.js` is CJS and inlines the same `camelToSnake` / `realmTypeToSql` / `EMBEDDED_SCHEMA_NAMES` logic that `SqliteUtils.js` and `SchemaGenerator.js` use. If you change type conversion in one place, change it in the other — the schema-validation test catches drift, but only if you run it.
- **Don't edit old migrations.** Once a migration is in `src/framework/db/migrations/drizzleMigrations.js` and released, it has run on users' devices. Editing it won't re-run — write a forward migration instead.

## Query Translation & JS Fallback (briefly)

`RealmQueryParser` handles ~95% of query patterns in straight SQL (simple filters, AND/OR/NOT, dot-notation FK, multi-level joins, IN, CONTAINS, sort, limit). Patterns it can't translate — `SUBQUERY`, nested `SUBQUERY`, `@count`, `@size`, `ANY`, `TRUEPREDICATE DISTINCT`, `@links.@count` — are flagged and evaluated post-hydration by `JsFallbackFilterEvaluator`. See [`RuleEngineCompatibility.md`](./RuleEngineCompatibility.md) for the full list and when to expect a perf cliff (JS fallback hydrates the full candidate set).

## Report-Card Fast Path

Rules that only need a count + a clickable line list should use `params.db.execReport(countSql, countParams, listSql, listParams, schema)` when `params.db.isSqlite` is true. The count runs immediately (~ms); the list is hydrated lazily on card tap. Full API and snake-case conventions in [`RuleSqlMigrationGuide.md`](./RuleSqlMigrationGuide.md).

## Testing

- **Dual-backend harness:** `test/helpers/dualBackendTestHarness.js` exports `BACKENDS = [['realm'], ['sqlite']]` for use with `describe.each()`. Used in `BaseServiceRepositoryTest`, `ServiceWriteMigrationTest`. Opt any new repository-touching test into it by parameterising on backend type — one test, both backends.
- **DB comparison scripts:**
  - `packages/openchs-android/src/utility/compareRealmAndSqlite.js` — row counts, sync status, missing rows across a captured Realm and SQLite DB.
  - `packages/openchs-android/src/utility/compareRealmDatabases.js` — two Realms (for rollback verification).
- **Perf benchmarks:** `test/perf/PerformanceBenchmarkTest.js`, `RuleQueryBenchmarkTest.js`, `SyncPersistBenchmarkTest.js`. Run on-device to time Realm vs SQLite on the same dataset.
- **Make targets:**
  ```
  make get_db_force          # pull Realm DB
  make get_sqlite_db         # pull SQLite DB (+ WAL checkpoint)
  make put_db / put_sqlite_db# push a captured DB back to a device for repro
  ```

**Open validation work:** sync performance still needs a production-workload comparison (Realm vs SQLite) on representative org data. Don't conclude on perf from dev-machine runs alone.

## Observability

- **Slow-query logging.** `SqliteProxy.slowQueryThreshold` (default 100ms) — queries over the threshold log a warning with elapsed time and row count via `General.logWarn("SqliteProxy", ...)`.
- **Query logging toggle.** `SqliteProxy.setLogQueries(true)` dumps every SQL it runs. Off by default; use locally when debugging a specific flow.
- **Sync telemetry.** `SyncTelemetryActions` now records `activeBackend: "realm" | "sqlite"` on the sync-completion payload. Entity counts in sync telemetry use `SELECT COUNT(*)` rather than hydrating results.

## Known Footguns

Things a dev who hasn't worked on this code is likely to trip over:

1. **Partial-object upserts can silently overwrite existing data.** If you `General.pick(entity, ['uuid', 'someList'])` and pass that to a write, `flatten()` used to include every schema property with its default (e.g. `observations: "[]"`), causing `COALESCE`-based upsert to wipe prior values. Fixed in 4433c2d65 — but the shape of the problem applies to any code path that hand-crafts partial objects. Verify the write only writes what you intend.
2. **FK preload into the session cache at depth-0 can hide deeper hydrations.** `EntityHydrator._batchPreloadFkReferences` used to cache FK'd entities without their child lists. When the same entity was later batch-loaded at depth 2 (e.g. `FormElementGroup` with `formElements` populated), the depth-0 stub won the cache lookup and the form rendered with no children. Fixed in 9cb6ef5a0 — but if you extend the preloader, respect the batch-load queue.
3. **Mutating managed Realm objects outside a write block.** `SubjectMigrationService.markMigrated` had a `hasMigrated = true` assignment drift outside the `transactionManager.write()` block after a refactor and threw. Rule of thumb: when you migrate a service onto the repository layer, check every property assignment sits inside a `transactionManager.write(...)`.
4. **`DrizzleSchemaExport.js` duplicates type-conversion logic.** It's a CJS module consumed by `drizzle-kit` at generation time, so it can't import the ESM helpers from `SqliteUtils.js`. Keep the two in sync when you change a type mapping — the schema-validation test will catch drift, but only after you re-run `make migration_export_schema`.
5. **Op-sqlite WAL files travel with the DB.** `make get_sqlite_db` does `PRAGMA wal_checkpoint(TRUNCATE)` and deletes the `-wal` / `-shm` files for you. If you pull the DB with plain `adb pull`, you may see stale data — checkpoint first.

## Where to Start Reading

If you're new to this code and need to get productive fast:

1. `GlobalContext.initialiseGlobalContext()` — how the backends are wired up.
2. `SqliteProxy.js` — the Realm contract implemented on SQLite. Read `objects()`, `create()`, `write()`, `bulkCreate()`.
3. `SqliteResultsProxy.js` — understand that query results are lazy.
4. `EntityHydrator.js` — how flat rows become objects. Where the session cache and reference cache live.
5. `RealmQueryParser.js` — how query strings become SQL. Grep for the pattern you're debugging.
6. `SqliteMigrationService.js` — the activation state machine.
7. `SyncService.js` — search for `isSqlite` to see every branch where the paths diverge.

