---
- name: Install android-sdk
  package: name=android-sdk state=latest
  tags:
    - android
    - brew
    - apt

- name: Install required Android-SDK Libs
  command: "echo y | ./android update sdk --no-ui --all --filter {{ item }}"
  with_items: android_deps